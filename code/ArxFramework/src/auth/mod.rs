pub mod auth_core;

pub fn initialize() -> Result<(), String> {
    // Qui puoi mettere il codice di inizializzazione specifico per il modulo auth
    println!("Auth module initialized");
    Ok(())
}
